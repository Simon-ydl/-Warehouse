server:
  port: 7002
spring:
  application:
    name: yjs-eureka #微服名称
eureka:
  client:
    register-with-eureka: false # 是否将该服务注册到eureka服务端
    fetch-registry: false # 是否从eureka服务端获取信息
    service-url: # eureka的注册地址
      defaultZone: http://10.147.2.170:7002/eureka